name: one_kopter_mpc
root: ./
startup_window: roscore
pre_window: export UAV_NAMESPACE=red
windows:
  - roscore:
      layout: tiled
      panes:
        - roscore
        - export SITL_RITW_TERMINAL="tmux new-window -n:ardupilot1"; waitForRos; roslaunch ardupilot_gazebo sim_vehicle.launch enable_console:=false
        - waitForRos; rosparam set use_sim_time true; roslaunch ardupilot_gazebo mavros.launch 
  - gazebo:
      layout: tiled
      panes:
        - waitForRos; roslaunch ardupilot_gazebo kopterworx.launch use_sim_time:=true gui:=true
  - arm&takeof:
      layout: tiled
      panes:
        - waitForRos; roslaunch uav_ros_control control_manager.launch manual_takeoff:=false
        - rosrun ardupilot_gazebo automatic_takeoff.sh
  - trajectory:
      layout: tiled
      panes:
        - waitForRos; roslaunch uav_ros_tracker mpc_tracker.launch tracker_config:=custom_config/mpc_config.yaml
  - plotter:
      layout: tiled
      panes:
        - waitForRos; sleep 10; rosrun rqt_gui rqt_gui --perspective-file custom_config/mpc_debug.perspective
